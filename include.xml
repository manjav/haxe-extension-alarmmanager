<?xml version="1.0" encoding="utf-8"?>
<extension>
	
	<ndll name="alarmmanager" />
	
	<!-- Use the following for an Android Java extension, not needed otherwise -->
	<section if="android">
		<dependency name="alarmmanager" path="dependencies/android" if="android" />
	</section>
	
	<android extension="com.gerantech.extension.alarms.AlarmsExtension" />
	
</extension>